Git repo unit
1. git init (Master is the main branch.)
2. git add license.txt README.md
3. git status
4. git status -s
5. git add .
6. git status -s
7. git commit -m "init commit"
8. git status --help
9. git -h
10. git commit -a -m "new changes"


git ignoring
1. git -r --caches bin/
2. touch .gitignore
3. nano .gitignore CTRL+X
4. git commit -m "Added .gitignore and remove bin/from staging"
5. 

Git undoing things & Vi text editor
1. git commit --ammend
2. git log -1
3. git config --global core.editor "vi" I Key :wq
4. git commit --global core.editor "notepad"
5. git add .
6. git commit --amend --no-edit
7. touch someImportantConfigs.txt
8. git restore --staged src/HelloWorld.java

git hosting & Remote repositories
1. git remote -v
2. git remote add origin https://github.com/mohdnadeemquamar/demo.git (to add git online)
3. git push origin -u master (refresh your web page git id)
4. git clone https://github.com/mohdnadeemquamar/demo.git (clone your repo)

SSH Connection
Add SSH key in your github profile in ssh and gpg key. Goto Git GUI and copy
ssh key from help. And then create a new ssh key and paste there.
git remote set-url origin git@github.com
git commit -a-m "test SSH conection"
git push origin master

git branching
1. git branch TA0001
2. git branch 
3. git bracnh -r
4. git branch -a
5. git checkout
6. git checkout -b TA0001_discount_calculation
7. cd src
8. touch DiscountcalculationService.java
9. 

















